<%- include('partials/app-header') %>
    <div class="max-w-7xl mx-auto py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-3xl mx-auto text-center">
            <h1 class="text-3xl font-extrabold text-red-600 sm:text-4xl">
                Error Occurred
            </h1>
            <p class="mt-4 text-lg text-gray-500">
                <%= locals.message || 'Something went wrong. Please try again later.' %>
            </p>
            <div class="mt-6">
                <a href="/login" class="text-base font-medium text-blue-600 hover:text-blue-500">
                    Go back to login <span aria-hidden="true"> &rarr;</span>
                </a>
            </div>
        </div>
    </div>
    <%- include('partials/app-footer') %>