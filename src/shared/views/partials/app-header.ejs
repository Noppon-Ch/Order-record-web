<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Record</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script nonce="<%= typeof nonce !== 'undefined' ? nonce : '' %>">
        // Inject server-side access token into global scope
        window.USER_ACCESS_TOKEN = "<%= (typeof user !== 'undefined' && user && user.access_token) ? user.access_token : '' %>";
    </script>
    <script src="/js/utils/api.js" defer></script>
</head>

<body
    class="bg-gray-50 min-h-screen flex flex-col text-gray-800 antialiased selection:bg-blue-100 selection:text-blue-900">
    <%- include('navbar') %>

        <main
            class="flex-1 flex flex-col items-center justify-center <%= (typeof fullWidth !== 'undefined' && fullWidth) ? '' : 'p-0 sm:p-4' %>">
            <div class="w-full">