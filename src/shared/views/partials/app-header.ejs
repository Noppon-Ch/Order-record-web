<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Record</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body
    class="bg-gray-50 min-h-screen flex flex-col text-gray-800 antialiased selection:bg-blue-100 selection:text-blue-900">
    <%- include('navbar') %>

        <main
            class="flex-1 flex flex-col items-center justify-center <%= (typeof fullWidth !== 'undefined' && fullWidth) ? '' : 'p-0 sm:p-4' %>">
            <div class="w-full">