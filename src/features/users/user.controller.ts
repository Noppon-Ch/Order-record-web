export function renderUserSetting(userId: string): string {
    // Implementation to render user settings
    // This is a placeholder; replace with actual logic
    return `Rendering settings for user ${userId}`;
}