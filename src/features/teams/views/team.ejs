<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Team | Order Record</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.js"></script>
</head>

<body class="bg-gray-50 flex flex-col min-h-screen">

    <%- include('../../../shared/views/partials/navbar') %>

        <main class="flex-grow container mx-auto px-4 py-8">
            <div class="max-w-4xl mx-auto">

                <div class="mb-8 flex justify-between items-center">
                    <h1 class="text-2xl font-bold text-gray-800">จัดการทีม</h1>
                </div>

                <!-- Current Team Status -->
                <% if (userTeam && userTeam.team) { %>
                    <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-6 mb-8 relative overflow-hidden">
                        <div class="absolute top-0 right-0 p-4 opacity-10">
                            <svg class="w-32 h-32 text-indigo-600" fill="currentColor" viewBox="0 0 20 20">
                                <path
                                    d="M13 6a3 3 0 11-6 0 3 3 0 016 0zM18 8a2 2 0 11-4 0 2 2 0 014 0zM14 15a4 4 0 00-8 0v3h8v-3zM6 8a2 2 0 11-4 0 2 2 0 014 0zM16 18v-3a5.972 5.972 0 00-.75-2.906A3.005 3.005 0 0119 15v3h-3zM4.75 12.094A5.973 5.973 0 004 15v3H1v-3a3 3 0 013.75-2.906z">
                                </path>
                            </svg>
                        </div>

                        <div class="relative z-10">
                            <div class="flex items-center gap-4 mb-4">
                                <div
                                    class="w-16 h-16 rounded-full bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center text-white text-2xl font-bold shadow-md">
                                    <%= userTeam.team.team_name.charAt(0).toUpperCase() %>
                                </div>
                                <div>
                                    <h2 class="text-xl font-bold text-gray-900">
                                        <%= userTeam.team.team_name %>
                                    </h2>
                                    <p class="text-sm text-gray-500">Code: <span
                                            class="font-mono bg-gray-100 px-2 py-0.5 rounded text-gray-700 select-all">
                                            <%= userTeam.team.team_code %>
                                        </span></p>
                                </div>
                                <div class="ml-auto">
                                    <span
                                        class="px-3 py-1 rounded-full text-sm font-medium 
                                    <%= userTeam.member.role === 'leader' ? 'bg-indigo-100 text-indigo-700' : 'bg-blue-100 text-blue-700' %>">
                                        <%= userTeam.member.role==='leader' ? 'Leader' : 'Member' %>
                                    </span>
                                </div>
                            </div>

                            <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6">
                                <div class="bg-gray-50 p-4 rounded-lg">
                                    <p class="text-sm text-gray-500 mb-1">สถานะสมาชิก</p>
                                    <p class="font-semibold text-gray-700 capitalize">
                                        <%= userTeam.member.status %>
                                    </p>
                                </div>
                                <div class="bg-gray-50 p-4 rounded-lg">
                                    <p class="text-sm text-gray-500 mb-1">วันที่เข้าร่วม</p>
                                    <p class="font-semibold text-gray-700">
                                        <%= new Date(userTeam.member.joined_at).toLocaleDateString('th-TH') %>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                    <% } else { %>
                        <!-- No Team State -->
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">

                            <!-- Join Team -->
                            <div
                                class="bg-white rounded-xl shadow-sm border border-gray-100 p-6 flex flex-col h-full hover:shadow-md transition-shadow">
                                <div class="mb-4 text-indigo-600">
                                    <svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z">
                                        </path>
                                    </svg>
                                </div>
                                <h3 class="text-lg font-bold text-gray-900 mb-2">เข้าร่วมทีม</h3>
                                <p class="text-gray-500 text-sm mb-6 flex-grow">
                                    ค้นหาทีมที่มีอยู่แล้วเพื่อเข้าร่วมทำงานกับผู้อื่น</p>

                                <form id="joinTeamForm" class="space-y-4">
                                    <div>
                                        <label for="teamCode"
                                            class="block text-sm font-medium text-gray-700 mb-1">รหัสทีม (Team
                                            Code)</label>
                                        <input type="text" id="teamCode" name="teamCode" required
                                            class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 outline-none transition-colors"
                                            placeholder="เช่น AB1234">
                                    </div>
                                    <button type="submit"
                                        class="w-full py-2.5 px-4 bg-white border border-indigo-600 text-indigo-600 font-semibold rounded-lg hover:bg-indigo-50 transition-colors">
                                        เข้าร่วมทีม
                                    </button>
                                </form>
                            </div>

                            <!-- Create Team -->
                            <div
                                class="bg-white rounded-xl shadow-sm border border-gray-100 p-6 flex flex-col h-full hover:shadow-md transition-shadow">
                                <div class="mb-4 text-purple-600">
                                    <svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                                    </svg>
                                </div>
                                <h3 class="text-lg font-bold text-gray-900 mb-2">สร้างทีมใหม่</h3>
                                <p class="text-gray-500 text-sm mb-6 flex-grow">
                                    สร้างพื้นที่ทำงานของคุณเองและเชิญสมาชิกมาร่วมทีม</p>

                                <button id="openCreateTeamBtn"
                                    class="w-full py-2.5 px-4 bg-gradient-to-r from-indigo-600 to-purple-600 text-white font-semibold rounded-lg hover:shadow-lg hover:scale-[1.02] transition-all duration-200">
                                    สร้างทีม
                                </button>
                            </div>

                        </div>

                        <!-- Create Team Modal -->
                        <div id="createTeamModal" class="fixed inset-0 z-50 hidden overflow-y-auto"
                            aria-labelledby="modal-title" role="dialog" aria-modal="true">
                            <div
                                class="flex items-center justify-center min-h-screen px-4 pt-4 pb-20 text-center sm:block sm:p-0">

                                <div class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity"
                                    aria-hidden="true" id="closeCreateTeamOverlay"></div>

                                <span class="hidden sm:inline-block sm:align-middle sm:h-screen"
                                    aria-hidden="true">&#8203;</span>

                                <div
                                    class="inline-block align-bottom bg-white rounded-lg text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full relative z-50">
                                    <div class="bg-white px-4 pt-5 pb-4 sm:p-6 sm:pb-4">
                                        <div class="sm:flex sm:items-start">
                                            <div
                                                class="mx-auto flex-shrink-0 flex items-center justify-center h-12 w-12 rounded-full bg-purple-100 sm:mx-0 sm:h-10 sm:w-10">
                                                <svg class="h-6 w-6 text-purple-600" fill="none" stroke="currentColor"
                                                    viewBox="0 0 24 24">
                                                    <path stroke-linecap="round" stroke-linejoin="round"
                                                        stroke-width="2"
                                                        d="M12 9v3m0 0v3m0-3h3m-3 0H9m12 0a9 9 0 11-18 0 9 9 0 0118 0z">
                                                    </path>
                                                </svg>
                                            </div>
                                            <div class="mt-3 text-center sm:mt-0 sm:ml-4 sm:text-left w-full">
                                                <h3 class="text-lg leading-6 font-medium text-gray-900"
                                                    id="modal-title">สร้างทีมใหม่</h3>
                                                <div class="mt-2">
                                                    <p class="text-sm text-gray-500 mb-4">
                                                        กรุณาระบุชื่อทีมของคุณเพื่อเริ่มต้นใช้งาน
                                                        (สามารถเปลี่ยนแปลงได้ภายหลัง)</p>
                                                    <form id="createTeamForm">
                                                        <label for="teamName"
                                                            class="block text-sm font-medium text-gray-700 mb-1">ชื่อทีม</label>
                                                        <input type="text" id="teamName" name="teamName" required
                                                            class="w-full px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-purple-500 focus:border-purple-500 outline-none"
                                                            placeholder="เช่น Dream Team">
                                                    </form>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="bg-gray-50 px-4 py-3 sm:px-6 sm:flex sm:flex-row-reverse">
                                        <button type="button" id="submitCreateTeamBtn"
                                            class="w-full inline-flex justify-center rounded-md border border-transparent shadow-sm px-4 py-2 bg-purple-600 text-base font-medium text-white hover:bg-purple-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-500 sm:ml-3 sm:w-auto sm:text-sm">
                                            ยืนยันสร้างทีม
                                        </button>
                                        <button type="button" id="cancelCreateTeamBtn"
                                            class="mt-3 w-full inline-flex justify-center rounded-md border border-gray-300 shadow-sm px-4 py-2 bg-white text-base font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 sm:mt-0 sm:ml-3 sm:w-auto sm:text-sm">
                                            ยกเลิก
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Search Teams Section (Optional Enhancement for joining) -->
                        <div class="mt-8 bg-white rounded-xl shadow-sm border border-gray-100 p-6 hidden"
                            id="searchSection">
                            <h3 class="text-lg font-bold text-gray-900 mb-4">ค้นหาทีม</h3>
                            <div class="flex gap-2 mb-4">
                                <input type="text" id="searchTeamInput" placeholder="พิมพ์ชื่อทีมเพื่อค้นหา..."
                                    class="flex-grow px-4 py-2 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-500 outline-none">
                                <button id="searchTeamsBtn"
                                    class="px-4 py-2 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200">ค้นหา</button>
                            </div>
                            <div id="searchResults" class="space-y-2"></div>
                        </div>

                        <% } %>

            </div>
        </main>

        <script nonce="<%= nonce %>">
            document.addEventListener('DOMContentLoaded', () => {
                // --- Safe DOM Elements ---
                const createTeamModal = document.getElementById('createTeamModal');
                const openCreateTeamBtn = document.getElementById('openCreateTeamBtn');
                const closeCreateTeamOverlay = document.getElementById('closeCreateTeamOverlay');
                const cancelCreateTeamBtn = document.getElementById('cancelCreateTeamBtn');
                const submitCreateTeamBtn = document.getElementById('submitCreateTeamBtn');
                const searchTeamsBtn = document.getElementById('searchTeamsBtn');

                // --- Modal Logic ---
                function openModal() {
                    if (createTeamModal) createTeamModal.classList.remove('hidden');
                }

                function closeModal() {
                    if (createTeamModal) createTeamModal.classList.add('hidden');
                }

                if (openCreateTeamBtn) openCreateTeamBtn.addEventListener('click', openModal);
                if (closeCreateTeamOverlay) closeCreateTeamOverlay.addEventListener('click', closeModal);
                if (cancelCreateTeamBtn) cancelCreateTeamBtn.addEventListener('click', closeModal);

                // --- API Interaction ---
                async function submitCreateTeam() {
                    const teamNameInput = document.getElementById('teamName');
                    const teamName = teamNameInput ? teamNameInput.value : '';

                    if (!teamName) {
                        return Swal.fire({
                            icon: 'warning',
                            title: 'กรุณาระบุชื่อทีม',
                            text: 'Team name is required',
                            confirmButtonColor: '#7c3aed'
                        });
                    }

                    try {
                        const response = await fetch('/teams/create', {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({ teamName })
                        });
                        const result = await response.json();

                        if (result.success) {
                            Swal.fire({
                                icon: 'success',
                                title: 'สร้างทีมสำเร็จ!',
                                text: 'Team created successfully',
                                confirmButtonColor: '#7c3aed'
                            }).then(() => {
                                window.location.reload();
                            });
                        } else {
                            Swal.fire({
                                icon: 'error',
                                title: 'เกิดข้อผิดพลาด',
                                text: result.message || 'Something went wrong',
                                confirmButtonColor: '#ef4444'
                            });
                        }
                    } catch (error) {
                        console.error(error);
                        Swal.fire({
                            icon: 'error',
                            title: 'Error',
                            text: 'Connection error',
                            confirmButtonColor: '#ef4444'
                        });
                    }
                }

                if (submitCreateTeamBtn) submitCreateTeamBtn.addEventListener('click', submitCreateTeam);

                // Join Team Form
                const joinTeamForm = document.getElementById('joinTeamForm');
                if (joinTeamForm) {
                    joinTeamForm.addEventListener('submit', async (e) => {
                        e.preventDefault();
                        const teamCodeInput = document.getElementById('teamCode');
                        const teamCode = teamCodeInput ? teamCodeInput.value : '';

                        try {
                            const response = await fetch('/teams/join', {
                                method: 'POST',
                                headers: { 'Content-Type': 'application/json' },
                                body: JSON.stringify({ teamCode })
                            });
                            const result = await response.json();

                            if (result.success) {
                                Swal.fire({
                                    icon: 'success',
                                    title: 'สำเร็จ!',
                                    text: 'เข้าร่วมทีมสำเร็จ (Joined team successfully)',
                                    confirmButtonColor: '#7c3aed'
                                }).then(() => {
                                    window.location.reload();
                                });
                            } else {
                                Swal.fire({
                                    icon: 'error',
                                    title: 'เกิดข้อผิดพลาด',
                                    text: result.message || 'Error joining team',
                                    confirmButtonColor: '#ef4444'
                                });
                            }
                        } catch (error) {
                            console.error(error);
                            Swal.fire({
                                icon: 'error',
                                title: 'Error',
                                text: 'Connection error',
                                confirmButtonColor: '#ef4444'
                            });
                        }
                    });
                }

                // Optional: Implement search logic
                if (searchTeamsBtn) {
                    searchTeamsBtn.addEventListener('click', searchTeams);
                }

                async function searchTeams() {
                    // ... implementation ...
                }
            });
        </script>
</body>

</html>