<%- include('main', { body: `
  <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-md text-center">
    <h1 class="text-3xl font-bold mb-6 text-gray-800">Welcome to Order Record Web</h1>
    <p class="mb-2 text-gray-600">You are logged in.</p>
    <p class="mb-8 text-blue-700 font-semibold">
      Registered Email: <span>${ email }</span>
    </p>
    <a href="/auth/logout">
      <button class="py-2 px-4 bg-red-600 text-white rounded hover:bg-red-700 transition">Logout</button>
    </a>
  </div>
` }) %>
